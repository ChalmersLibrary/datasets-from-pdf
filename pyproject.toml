[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "extract-dataset"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pymupdf>=1.19",
    "requests",
]

[project.scripts]
extract-dataset = "extract_dataset.cli:main"
